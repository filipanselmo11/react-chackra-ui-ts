import { CircularProgress, CircularProgressLabel } from '@chakra-ui/react'
import * as React from 'react'

export const Thickness = () => {
    return (
        <CircularProgress value={59} size={100} thickness={4}/>
    )
}